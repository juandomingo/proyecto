{% extends "base.html.twig" %}
{% block title %}Inicio{% endblock %}
{% block head %}{% endblock %}
{% block loginfo %}{{ parent() }}{% endblock %}
{% block content %}
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
        <script> 
            $(function(){
            $("#demo-bar").load("../demo-bar.html"); 
            });
        </script> 
        {% if detalles_alimentos | length > 0 %}
        	{% include "listDetallesAlimentosSinLinks.html.twig" %}
        {% else %}
    		<p class="importante"> No hay alimentos por vencer </p>
		{% endif %}

		{% if turnos_entrega_hoy | length > 0 %}
        	{% include "listTurnosEntregaHoy.html.twig" %}
        {% else %}
    		<p class="importante"> No hay entregas para hoy </p>
		{% endif %}	

{% endblock %}
{% block footer %}{{ parent() }}{% endblock %}